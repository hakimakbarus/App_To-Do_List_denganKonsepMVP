<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hakim.appto_dolist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme1">
        <activity android:name=".modul.edit_list_todo.EditListToDoActivity"></activity>
        <activity android:name=".modul.new_list_todo.NewListToDoActivity"></activity>
        <activity android:name=".modul.form_todo.FormToDoActivity"></activity>
        <activity android:name=".modul.list_todo.ListToDoActivity"></activity>
        <activity android:name=".modul.profile.ProfileActivity"></activity>
        <activity android:name=".modul.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>